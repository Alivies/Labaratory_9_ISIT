
#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include "windows.h"

#pragma warning(disable : 4996)

		printf("\n");
		printf("________________");
		printf("\n");

		M = (int*)malloc(sizeof(int) * n);

		fseek(f, 0, SEEK_SET);

		for (int i = 0; i < n; i++)
		{
			fscanf(f, "%c", &M[i]);
		}
		
		for (int i = 0; i < n; i++) {
			for (int j = i+1; j < n; j++) {
				if (M[i] == M[j]) {
					for (int k = j; k < n - 1; k++) {
						M[k] = M[k + 1];
					}
					n--;
				}
			}
		}

		


}